$("#moreNews").remove();
var boxes = "<%= j(render '/news_feeds/index', news_feeds: @news_feeds) %>";
$(".dashboard").append(boxes);
$(".dashboard").append("<%= j(render '/news_feeds/more_link', news_feeds: @news_feeds) %>");
if($("#moreNews a").length == 0)
  $("#moreNews").remove();
$("abbr.timeago").timeago();
$('html,body').animate({scrollTop: $("#nf<%= @news_feeds.first.id %>").offset().top-150},'slow');
setTimeout(function(){ $(".new").removeClass("new"); }, 3000);